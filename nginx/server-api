server {
  include security-custom;
  include security-custom-advance;
  include gzip-custom;
  listen 443 ssl http2;
  server_name api.sungryeol.com;

  location / {
    # return 200 "api";
    proxy_pass 'http://localhost:4000';
  }
}